```
Usage: HC256.exe [-t] [-h] [-k KEY] [-i IV] [-s STRING] [-o STRING]
        -t:             测试
        -h:             显示帮助
        -k KEY:         加密密钥，32位，默认为"000000000000"
        -i IV:          加密IV，32位，默认为"000000000000"
        -l NUMBER:      输出密码流长度(byte)，默认为1024
		-o STRING:      输出到文件名"
        -q bool:        只输出密码流"

编译平台：Windows VS2015
```
